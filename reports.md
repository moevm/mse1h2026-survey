# mse-template

## О проекте

### Постановка задачи

Система кафедральных опросов — это веб-приложение для проведения опросов, хранения результатов и анализа полученных данных.

Основные цели системы:

- проведение опросов через веб-интерфейс
- хранение результатов опросов в базе данных
- просмотр результатов опросов
- анализ данных
- визуализация статистики
- экспорт результатов

Система предоставляет:

- пользовательский интерфейс для прохождения опросов
- административную панель для управления опросами
- REST API для взаимодействия frontend и backend
- хранение данных в PostgreSQL

Проект является развитием существующей системы:

https://survey.moevm.info/

---

### Требования

Основные функциональные требования:

Система должна обеспечивать:

- создание опросов
- редактирование опросов
- удаление опросов
- прохождение опросов по уникальной ссылке
- хранение ответов
- просмотр статистики
- экспорт данных
- визуализацию данных

Типы вопросов:

- **free** — свободный ответ
- **checkbox** — множественный выбор
- **radiobutton** — один вариант ответа
- **scale** — оценка от 1..N

Технологии:

**Backend**

- FastAPI
- SQLAlchemy
- PostgreSQL
- Pydantic

**Frontend**

- React
- JavaScript

**Infrastructure**

- Docker
- nginx

Нефункциональные требования:

- поддержка не менее 100 одновременных пользователей
- время ответа API не более 5 секунд
- безопасная авторизация пользователей
- хранение паролей в зашифрованном виде
- модульная архитектура

---

### Сценарии использования

#### Прохождение опроса

1. Пользователь открывает ссылку вида:

`/survey/{survey_id}`

2. Система проверяет статус опроса.

3. Если опрос активен:

- отображается название
- отображается описание
- отображаются вопросы

4. Пользователь отвечает на вопросы.

5. Пользователь отправляет ответы.

6. Система сохраняет ответы в базе данных.

7. Отображается сообщение об успешной отправке.

---

#### Управление опросами (Администратор)

1. Администратор входит в систему.

2. Администратор открывает административную панель.

3. Администратор может:

- создать опрос
- редактировать опрос
- удалить опрос
- включить опрос
- отключить опрос

---

#### Просмотр статистики

1. Администратор открывает страницу статистики.

2. Система отображает:

- количество ответов
- список вопросов
- распределение ответов

3. Система строит графики.

---

### Инструкция по запуску

#### 1. Клонировать репозиторий

```bash
git clone https://github.com/moevm/mse1h2026-survey.git
```

#### 2. Перейти в каталог проекта

```bash
cd mse1h2026-survey
```

#### 3. Запустить контейнеры

##### Linux

```bash
sudo docker compose up --build
```

##### Windows

```bash
docker compose up --build
```

#### 4. Применить миграции базы данных

После запуска контейнеров открой новый терминал и выполни:

##### Linux

```bash
sudo docker compose exec backend alembic upgrade head
```

##### Windows

```bash
docker compose exec backend alembic upgrade head
```

Команду необходимо выполнять после полного запуска контейнеров.

#### 5. Открыть приложение

Frontend:
http://localhost:8080

Backend API:
http://localhost:8080/api

Swagger:
http://localhost:8080/api/docs

#### Остановка контейнеров

```bash
docker compose down
```

## Итерация №1

### Презентация

В первой итерации был разработан базовый каркас системы кафедральных опросов.

Реализованы:

- архитектура backend
- структура базы данных
- базовый API
- макеты интерфейса
- docker окружение
- настройка nginx
- прототип модуля статистики

Основной целью итерации являлась подготовка инфраструктуры проекта и создание минимальной рабочей версии системы.

---

### Скринкасты

---

### Запланированные задачи

В рамках первой итерации были запланированы следующие задачи:

1. Макет дизайна
2. Макет API
3. Проектирование структуры базы данных
4. Настройка подключения к базе данных
5. Подготовка Docker окружения + nginx
6. Прототип модуля визуализации статистики
7. Тестовая задача визуализации
8. Runtime-env

---

### Выполненные задачи

В первой итерации выполнены следующие задачи:

**Макет дизайна**

Разработан базовый макет пользовательского интерфейса системы.

**Макет API**

Определена структура REST API:

- получение опросов
- получение опроса
- создание опроса
- редактирование опроса
- удаление опроса
- отправка ответов

**Проектирование структуры базы данных**

Спроектированы основные таблицы:

- surveys
- answers
- users

**Настройка подключения к базе данных**

Реализовано подключение FastAPI к PostgreSQL.

**Подготовка Docker окружения + nginx**

Настроены:

- Dockerfile
- docker-compose
- nginx конфигурация

**Прототип модуля визуализации статистики**

Реализован прототип отображения статистических данных.

**Тестовая задача визуализации**

Реализован тестовый модуль визуализации.

**Runtime-env**

Настроено окружение выполнения приложения.

---

### Задачи на следующую итерацию

Во второй итерации планируется реализовать:

- авторизацию пользователей
- страницу прохождения опроса
- отправку ответов через API
- валидацию данных
- обработку ошибок

## Итерация №2

### Презентация

Во второй итерации был реализован основной пользовательский сценарий прохождения опроса и выполнена интеграция frontend и backend частей приложения.

Пользователь может:
- ввести данные (группу)
- открыть опрос
- пройти все типы вопросов (scale, radio, text)
- отправить ответы
- получить страницу завершения

Основной целью итерации являлась:
- интеграция frontend и backend
- реализация полного пользовательского сценария
- настройка инфраструктуры (Docker, nginx, БД)
- подготовка проекта к расширению функциональности

---

### Скринкасты

В рамках итерации был подготовлен скринкаст, демонстрирующий:
- запуск проекта через Docker
- работу frontend и backend
- прохождение опроса
- отправку ответов
- отображение страницы завершения

---

### Запланированные задачи (Итерация 2)

В рамках второй итерации были запланированы следующие группы задач:

1. Интеграция frontend и backend  
2. Реализация API для опросов и ответов  
3. Настройка инфраструктуры (Docker, nginx, база данных)  
4. Реализация пользовательского интерфейса  
5. Реализация системы авторизации и ролей  
6. Добавление валидации и тестирования  
7. Подготовка документации  

---

### Выполненные задачи (Итерация 2)

Во второй итерации были выполнены следующие группы задач:

#### Backend
- Реализован REST API для опросов и ответов
- Настроена работа с базой данных PostgreSQL
- Реализованы миграции
- Добавлена базовая авторизация и ролевая модель

#### Frontend
- Реализованы страницы:
  - стартовая страница
  - страница опроса
  - страница завершения
- Поддержка различных типов вопросов (scale, radio, text)
- Реализована навигация по вопросам

#### Интеграция
- Настроено взаимодействие frontend и backend через API
- Реализована отправка ответов на сервер

#### Инфраструктура
- Настроен Docker Compose
- Подключен nginx
- Настроен доступ к Swagger через `/api/docs`

#### Документация
- Добавлена базовая валидация данных
- Описан процесс запуска проекта в README

---

### Задачи на следующую итерацию (Итерация 3)

В следующей итерации планируется реализовать:

#### Расширение функциональности опросов
- добавление новых типов вопросов
- поддержка изображений в опросах
- привязка опросов к группам

#### Администрирование
- реализация админ-панели
- управление доступами (роли)
- просмотр результатов опросов

#### Экспорт данных
- экспорт результатов в PDF
- экспорт результатов в Excel

#### Улучшение архитектуры
- проработка структуры данных для экспорта
- оптимизация API

#### Качество проекта
- добавление тестов в процесс сборки
- расширение документации